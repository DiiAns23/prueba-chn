server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: las rutas del lado cliente (Angular Router) no existen como archivos,
  # así que cualquier ruta desconocida devuelve index.html.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
